===============================
32-0 Module Overview: Primitive vs Non-Primitive Data Types
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- Primitive data type কী?
- Non-primitive data type কী?
- Immutable বনাম Mutable এর পার্থক্য কী?
- Copy করলে মান পরিবর্তন হয় কীভাবে?

📝 Notes (বিস্তারিত তথ্য)

1. Primitive Data Types
- Primitive → একক মান (single value)।
- Immutable → মান পরিবর্তন করলে copy তে প্রভাব পড়ে না।
- উদাহরণ: string, number, boolean, null, undefined, symbol, bigint।

Example:
let person = "solim saheb";
let newPerson = person; // copy value
person = "hero alom";
console.log(newPerson); // solim saheb (immutable → পরিবর্তন হয়নি)

2. Non-Primitive Data Types
- Non-primitive → একাধিক মান (multiple values) রাখতে পারে।
- Mutable → মান পরিবর্তন করলে copy তে প্রভাব পড়ে।
- উদাহরণ: object, array, function।

Example:
let person2 = { name: "gias uddin" };
let newPerson2 = person2;
person2.name = "hero alom";
console.log(newPerson2); // { name: "hero alom" } (mutable → পরিবর্তন হয়েছে)

📌 Summary
- Primitive → single value, immutable।
- Non-primitive → multiple values, mutable।
- Primitive copy করলে নতুন মান পরিবর্তন হয় না।
- Non-primitive copy করলে reference থাকে, তাই মান পরিবর্তন হলে copy তেও পরিবর্তন হয়।


===============================
32-1 Null vs Undefined & Ways to Get Undefined
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- null কী?
- undefined কী?
- null vs undefined এর পার্থক্য কী?
- কোন কোন ক্ষেত্রে undefined পাওয়া যায়?

📝 Notes (বিস্তারিত তথ্য)

1. null in JavaScript
- null → একটি special value যা "empty" বা "no value" বোঝায়।
- typeof null → "object" (JavaScript এর bug/quirk)।
- Example:
  let name5 = null;
  console.log(name5);        // null
  name5 = "hero";
  console.log(name5);        // hero
  console.log(typeof name5); // object

2. undefined in JavaScript
- undefined → মান assign করা হয়নি বা পাওয়া যায়নি।
- typeof undefined → "undefined"।
- Example:
  let name3;
  console.log(name3); // undefined

3. Different Ways to Get Undefined
- Variable declare করা হয়েছে কিন্তু মান দেওয়া হয়নি।
  let x;
  console.log(x); // undefined

- Function parameter pass করা হয়নি।
  function sum(num1, num2) {
      console.log(num1, num2);
  }
  sum();   // undefined undefined
  sum(2);  // 2 undefined

- Function কিছু return করেনি।
  function sum(num1, num2) {
      console.log(num1 + num2);
  }
  const result = sum(10, 20);
  console.log(result); // undefined

- Function এ শুধু return; লেখা হয়েছে।
  function sum(num1, num2) {
      return;
  }
  console.log(sum(10, 20)); // undefined

- Object property নেই।
  const person = { name: "sofiul" };
  console.log(person.age); // undefined

- Reference error (variable নেই)।
  console.log(namesss); // ReferenceError → namesss is not defined

4. Example: sum function
function sum(num1, num2) {
    console.log(num1, num2);
}
console.log(sum(10, 20)); // 10 20 → undefined (কারণ return নেই)

📌 Summary
- null → "no value", manually assign করা হয়, typeof → object।
- undefined → "not assigned" বা "not found", typeof → undefined।
- Ways to get undefined:
  - Variable declare but not assigned।
  - Function parameter missing।
  - Function return না করা।
  - Object property না থাকা।
  - Reference error।


===============================
32-2 Different Truthy and Falsy Values in JavaScript
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- Truthy value কী?
- Falsy value কী?
- কোন কোন মান truthy আর কোনগুলো falsy?
- Boolean conversion (!, !!) কীভাবে কাজ করে?

📝 Notes (বিস্তারিত তথ্য)

1. Truthy Values
- যেকোনো মান যা Boolean এ রূপান্তর করলে true হয়।
- উদাহরণ:
  "SUBORNA ISLAM"   → truthy (non-empty string)
  "false"           → truthy (string, empty নয়)
  434               → truthy (positive number)
  -434              → truthy (negative number)
  {}                → truthy (empty object)
  {name:"gias"}     → truthy (non-empty object)
  []                → truthy (empty array)
  function(){}      → truthy (function)

2. Falsy Values
- যেকোনো মান যা Boolean এ রূপান্তর করলে false হয়।
- উদাহরণ:
  ""                → falsy (empty string)
  false             → falsy
  0                 → falsy
  null              → falsy
  undefined         → falsy
  NaN               → falsy

3. Example: Truthy Check
const name = "SUBORNA ISLAM";
if(name){
    console.log("this is truthy value");
} else {
    console.log("this is falsy value");
}
// Output: this is truthy value

4. Boolean Conversion
const nam = "sorna";
console.log(name == true); // false (string != boolean)
console.log(!name);        // false (negation → string truthy)
console.log(!!name);       // true (double negation → convert to boolean)

5. Example: Falsy Check
const naam = "";
if(!naam) console.log("true"); // Output: true

📌 Summary
- Truthy → non-empty string, non-zero number, object, array, function।
- Falsy → empty string, false, 0, null, undefined, NaN।
- !value → Boolean negation।
- !!value → Boolean conversion (true/false এ রূপান্তর)।


===============================
32-3 Double Equal (==) vs Triple Equal (===), Implicit Conversion
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- == এবং === এর মধ্যে পার্থক্য কী?
- Implicit type conversion কীভাবে কাজ করে?
- কোন ক্ষেত্রে == true হয় কিন্তু === false হয়?

📝 Notes (বিস্তারিত তথ্য)

1. Double Equal (==)
- শুধু value check করে।
- Type আলাদা হলেও implicit conversion করে compare করে।
- Example:
  const num1 = 10;
  const num2 = "10";
  console.log(num1 == num2);  // true (string "10" → number 10 এ convert হয়েছে)

2. Triple Equal (===)
- Value + Type দুটোই check করে।
- Type আলাদা হলে false হয়।
- Example:
  console.log(num1 === num2); // false (number vs string)

3. Boolean vs Number
const num3 = true;
const num4 = 1;
console.log(num3 == num4);  // true (true → 1 এ convert হয়েছে)
console.log(num3 === num4); // false (boolean vs number)

4. Array Comparison
const num5 = [];
const num6 = [];
console.log(num5 == num6);   // false (দুইটা আলাদা reference)
console.log(num5 === num6);  // false (reference আলাদা)

const num7 = [];
const num8 = num7;
console.log(num7 == num8);   // true (same reference)
console.log(num7 === num8);  // true (same reference)

5. Implicit Conversion Examples
- (5 * "2" === 10) → true ("2" string → number এ convert হয়েছে)
- "5" + null → "5null" (string concatenation)
- "5" - 2 → 3 (string "5" → number এ convert হয়েছে)

6. Summary Rule
- == → শুধু value check করে, type mismatch হলে implicit conversion করে।
- === → value + type check করে, type mismatch হলে false দেয়।

📌 Summary
- == → loose equality (value check only, type conversion হয়)।
- === → strict equality (value + type check, type mismatch হলে false)।
- Implicit conversion → JavaScript automatically type convert করে compare করে।


===============================
32-4 Block Scope, Global Scope & Hoisting
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- Block scope কী?
- Global scope কী?
- var বনাম let/const এর scope পার্থক্য কী?
- Hoisting কীভাবে কাজ করে?

📝 Notes (বিস্তারিত তথ্য)

1. Scope
- Scope → কোন variable কোথায় access করা যাবে তা নির্ধারণ করে।
- Global scope → function বা block এর বাইরে declare করা variable।
- Block scope → {} এর ভিতরে declare করা variable (let/const দিয়ে)।

Example:
function sum(num1, num2) {
    const result = num1 + num2;
    console.log(result);

    if(true) {
        var result3 = num1 - num2;   // var → block scope এ সীমাবদ্ধ নয়
        const result2 = num1 - num2; // const → block scope এ সীমাবদ্ধ
    }

    console.log(result2); // Error → block scope এর বাইরে access করা যায় না
    console.log(result3); // কাজ করবে → var block scope এ সীমাবদ্ধ নয়
}

console.log(result); // Error → function scope এর বাইরে access করা যায় না
sum(1, 2);

2. Hoisting
- Hoisting → JavaScript এ variable এবং function declaration উপরে তুলে নেয়া হয়।
- var → hoisted হয়, কিন্তু value assign হওয়ার আগে undefined থাকে।
- let/const → hoisted হয় কিন্তু Temporal Dead Zone এ থাকে, তাই assign হওয়ার আগে access করলে error হয়।

Example:
console.log(output); // undefined (কারণ var hoisted হয়েছে কিন্তু value assign হয়নি)
var output = "BD";

console.log(output); // Error (const hoisted হয়েছে কিন্তু assign হওয়ার আগে access করা যাবে না)
const output = "BD";

📌 Summary
- var → function scoped, block scope এ সীমাবদ্ধ নয়, hoisting হলে undefined হয়।
- let/const → block scoped, hoisting হলে error দেয় (Temporal Dead Zone)।
- Global scope → function/block এর বাইরে declare করা variable।
- Block scope → {} এর ভিতরে declare করা variable (let/const দিয়ে)।
- Hoisting → declaration উপরে যায়, কিন্তু initialization উপরে যায় না।


===============================
32-5 (Advanced) Explore about Closure
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- Closure কী?
- কেন Closure দরকার হয়?
- Function এর ভিতরের variable বাইরে থেকে access করা যায় কীভাবে?
- Closure এর practical use কোথায় হয়?

📝 Notes (বিস্তারিত তথ্য)

1. Closure Definition
- Closure হলো একটি function যেটি তার outer function এর variable access করতে পারে, এমনকি outer function শেষ হয়ে গেলেও।
- অর্থাৎ inner function তার parent scope এর variable মনে রাখে।

2. Example Without Closure
let counter = 0;
function sum() {
    counter = counter + 1;
    console.log(counter);
}
sum(); // 1
sum(); // 2
sum(); // 3
// এখানে counter global scope এ আছে, তাই সব function call এ একই counter update হচ্ছে।

3. Example With Local Variable (No Closure)
function sum() {
    let counter = 0;
    counter = counter + 1;
    console.log(counter);
}
sum(); // 1
sum(); // 1
sum(); // 1
// এখানে counter function scope এ আছে, তাই প্রতিবার নতুন করে initialize হচ্ছে।

4. Example With Closure
function sum() {
    let counter = 0;
    return function() {
        counter++;
        console.log(counter);
    }
}

const result = sum();
result(); // 1
result(); // 2
result(); // 3
result(); // 4
result(); // 5
// এখানে counter outer function এ আছে, কিন্তু inner function তাকে মনে রাখছে → Closure।

const result2 = sum();
result2(); // 1
result2(); // 2
// নতুন করে sum() call করলে নতুন counter তৈরি হয়।

5. Key Points
- Closure inner function কে outer function এর variable access করতে দেয়।
- প্রতিটি closure এর নিজস্ব scope থাকে।
- Closure data encapsulation এর জন্য ব্যবহার হয় (যেমন private variable তৈরি করা)।
- Practical use:
  - Counter তৈরি করা।
  - Private data hide করা।
  - Function factory তৈরি করা।

📌 Summary
- Closure → inner function তার parent scope এর variable মনে রাখে।
- Global variable → সব function share করে।
- Local variable → প্রতিবার নতুন করে initialize হয়।
- Closure → data persist করে, তাই call করলে আগের মান ধরে রাখে।



===============================
32-6 (Optional) Callback Function & Passing Different Functions
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- Callback function কী?
- Function কে argument হিসেবে pass করা যায় কীভাবে?
- কেন callback দরকার হয়?

📝 Notes (বিস্তারিত তথ্য)

1. Callback Function Definition
- Callback function হলো এমন একটি function যেটি অন্য একটি function এর argument হিসেবে pass করা হয়।
- মূল function সেই callback কে call করে কাজ সম্পন্ন করে।
- JavaScript এ asynchronous কাজ (যেমন API call, event handling) এর জন্য callback খুব গুরুত্বপূর্ণ।

2. Simple Example
function ghotok(patri, name) {
    patri(name);
}

const patri1 = function(name) {
    console.log("okay ami patri", name);
};

const patri2 = function(name) {
    console.log("okay ami patri", name);
};

const patri3 = function(name) {
    console.log("okay ami patri", name);
};

ghotok(patri1, "halima"); // okay ami patri halima
ghotok(patri2, "hania");  // okay ami patri hania
ghotok(patri3, "jorina"); // okay ami patri jorina

3. Key Points
- Function কে variable এর মতো ব্যবহার করা যায়।
- Function কে অন্য function এ argument হিসেবে pass করা যায়।
- Callback function মূলত অন্য function এর ভিতরে call হয়।
- Different callback দিলে output আলাদা হয়।

4. Practical Use Cases
- Event handling → button click হলে callback function execute হয়।
- Asynchronous কাজ → API থেকে data আসলে callback function দিয়ে handle করা হয়।
- Array methods → map, filter, forEach এ callback function ব্যবহার হয়।

📌 Summary
- Callback function → অন্য function এ argument হিসেবে pass করা হয়।
- ghotok() → patri নামের callback function কে call করে।
- Different callback দিলে different কাজ হয়।
- Callback asynchronous programming এ খুব গুরুত্বপূর্ণ।


===============================
32-7 (Advanced) Function Arguments: Pass by Value vs Pass by Reference
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- Pass by value কী?
- Pass by reference কী?
- Primitive vs Non-primitive data type function এ কীভাবে behave করে?
- arguments object কীভাবে কাজ করে?

📝 Notes (বিস্তারিত তথ্য)

1. Function Arguments & `arguments` Object
function sum(a, b, c) {
    const newArg = [...arguments];
    console.log(newArg);
}
console.log(sum.length);  // 3 (parameters count)
console.log(typeof sum);  // function
sum(10, 20, 30, 45, 64, 44, 53, 35, 23);
// Output: [10, 20, 30, 45, 64, 44, 53, 35, 23]

⚠️ Note:
- `arguments` object → function এ pass করা সব arguments কে ধরে।
- `arguments.push()` করা যায় না, কারণ এটি array নয় (array-like object)।

2. Pass by Value (Primitive Types)
let name = "masud";
function test(text) {
    test = "karim";
}
test(name);
console.log(name); // masud (primitive → pass by value)

let a = 10;
let b = 20;
function sums(num1, num2) {
    num1 = 50;
}
sums(a, b);
console.log(a); // 10 (primitive → original value পরিবর্তন হয়নি)

👉 Primitive types (string, number, boolean, null, undefined, symbol, bigint) সবসময় pass by value হয়।

3. Pass by Reference (Non-Primitive Types)
const person = { name: "rakib", age: 35 };
function sum2(obj) {
    obj.name = "ratul";
}
sum2(person);
console.log(person); // { name: "ratul", age: 35 }

👉 Non-primitive types (object, array, function) সবসময় pass by reference হয়।  
👉 Function এ modify করলে original object পরিবর্তন হয়।

4. Key Differences
- Pass by Value → copy যায়, original পরিবর্তন হয় না।
- Pass by Reference → reference যায়, original পরিবর্তন হয়।

📌 Summary
- Primitive data type → pass by value → copy যায় → original অপরিবর্তিত থাকে।
- Non-primitive data type → pass by reference → reference যায় → original পরিবর্তিত হয়।
- `arguments` object → function এ pass করা সব arguments ধরে, কিন্তু এটি array নয়।


===============================
32-7-1 Pass by Value vs Pass by Reference
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- Pass by value কী?
- Pass by reference কী?
- Primitive vs Non-primitive data type function এ কীভাবে behave করে?
- কখন কোনটা ব্যবহার করবো?

📝 Notes (বিস্তারিত তথ্য)

1. Example: Pass by Value
function changeNumber(num) {
    num = num + 10;
    console.log("function er vitore", num); // 20
}
const myNumber = 10;
changeNumber(myNumber);
console.log("function er baire", myNumber); // 10

👉 এখানে `myNumber` primitive type (number)। Function এ copy গেছে, তাই বাইরে আসল মান পরিবর্তন হয়নি।

2. Characteristics: Pass by Value
- Function আসল data এর একটি copy পায়।
- Function এর ভিতরে পরিবর্তন করলে আসল data তে প্রতিফলিত হয় না।
- JavaScript primitive data type (number, string, boolean, null, undefined, symbol, bigint) → pass by value।

3. Example: Pass by Reference
const person = { name: "rakib", age: 35 };
function changePerson(obj) {
    obj.name = "ratul";
}
changePerson(person);
console.log(person); // { name: "ratul", age: 35 }

👉 এখানে `person` object। Function এ reference গেছে, তাই আসল object পরিবর্তিত হয়েছে।

4. Characteristics: Pass by Reference
- Function আসল data এর reference পায়।
- Function এর ভিতরে পরিবর্তন করলে আসল data তে প্রতিফলিত হয়।
- JavaScript object, array, function → pass by reference।

5. কখন কোনটা ব্যবহার করবো
- Pass by Value → যখন নিশ্চিত করতে চান যে function এর কাজ আসল data পরিবর্তন করবে না।
- Pass by Reference → যখন function এর কাজ আসল object এ প্রতিফলিত করতে চান।

6. Practical Note
- JavaScript এ primitive data type বেশি ব্যবহৃত হয় → তাই pass by value বেশি দেখা যায়।
- Object/array এর সাথে কাজ করার সময় pass by reference জানা জরুরি।

📌 Summary
- Primitive → pass by value → copy যায় → আসল data অপরিবর্তিত।
- Non-primitive → pass by reference → reference যায় → আসল data পরিবর্তিত।
- Pass by value → নিরাপদ, আসল data পরিবর্তন হয় না।
- Pass by reference → powerful, আসল object পরিবর্তন হয়।


===============================
32-8 Pre Increment, Post Increment, Pre Decrement, Post Decrement
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- Pre increment/post increment কী?
- Pre decrement/post decrement কী?
- এদের মধ্যে পার্থক্য কীভাবে বোঝা যায়?

📝 Notes (বিস্তারিত তথ্য)

1. Increment (মান বাড়ানো)
let age = 10;

// age = age + 1;
// age += 1;
// age++;

console.log(age++); // 10 → Post Increment (আগে মান দেখায়, পরে বাড়ায়)
console.log(++age); // 11 → Pre Increment (আগে বাড়ায়, পরে মান দেখায়)

👉 Post Increment (x++) → প্রথমে পুরনো মান return করে, তারপর মান ১ বাড়ায়।
👉 Pre Increment (++x) → প্রথমে মান ১ বাড়ায়, তারপর নতুন মান return করে।

2. Decrement (মান কমানো)
let age2 = 10;

// age2 = age2 - 1;
// age2 -= 1;
// age2--;

console.log(age2--); // 10 → Post Decrement (আগে মান দেখায়, পরে কমায়)
console.log(--age2); // 9  → Pre Decrement (আগে কমায়, পরে মান দেখায়)

👉 Post Decrement (x--) → প্রথমে পুরনো মান return করে, তারপর মান ১ কমায়।
👉 Pre Decrement (--x) → প্রথমে মান ১ কমায়, তারপর নতুন মান return করে।

3. Key Differences
- Pre Increment (++x) → আগে বাড়ায়, পরে মান দেখায়।
- Post Increment (x++) → আগে মান দেখায়, পরে বাড়ায়।
- Pre Decrement (--x) → আগে কমায়, পরে মান দেখায়।
- Post Decrement (x--) → আগে মান দেখায়, পরে কমায়।

📌 Summary
- Increment → মান ১ বাড়ানো।
- Decrement → মান ১ কমানো।
- Pre → আগে পরিবর্তন হয়, পরে মান return হয়।
- Post → আগে মান return হয়, পরে পরিবর্তন হয়।

32-9 Module summary and interview questions

Interview Questions & Answers (JavaScript)

1. প্রশ্ন: জাভাস্ক্রিপ্ট কী কী ডাটা টাইপের ভেরিয়েবল আছে?
   উত্তর: 
   Primitive Types → string, number, bigint, boolean, undefined, symbol, null
   Non-Primitive Types → object (array, function, date ইত্যাদি)

2. প্রশ্ন: জাভাস্ক্রিপ্ট ফাংশন, বা array কি টাইপের জিনিস?
   উত্তর:
   Function → object
   Array → object (special form, index-based)

3. প্রশ্ন: জাভাস্ক্রিপ্ট এ array যদি অবজেক্ট হয় তাহলে কিভাবে চেক করবে কোন একটা ভেরিয়েবল একটা array নাকি array না?
   উত্তর:
   Array.isArray(variable)   // সবচেয়ে নির্ভরযোগ্য
   variable instanceof Array // কাজ করে, কিন্তু সবসময় নয়

4. প্রশ্ন: undefined আর null এর মধ্যে ডিফারেন্স কি?
   উত্তর:
   undefined → declare করা হয়েছে কিন্তু value নেই
   null → ইচ্ছা করে empty value দেওয়া হয়েছে

5. প্রশ্ন: double equal (==) আর triple equal (===) এই দুইটার মধ্যে ডিফারেন্স কি?
   উত্তর:
   ==  → loose equality (type conversion করে compare করে)
   === → strict equality (type ও value দুটোই match করতে হবে)

6. প্রশ্ন: implicit conversion কি জিনিস? এটা কখন হয়?
   উত্তর:
   জাভাস্ক্রিপ্ট স্বয়ংক্রিয়ভাবে type পরিবর্তন করে।
   উদাহরণ:
   "5" + 2 → "52"
   "5" - 2 → 3

7. প্রশ্ন: জাভাস্ক্রিপ্ট এ কয়েক ধরনের স্কোপ আছে। এই স্কোপ গুলার মধ্যে ডিফারেন্স কি?
   উত্তর:
   Global Scope → পুরো প্রোগ্রামে accessible
   Function Scope → ফাংশনের ভিতরে declare করা ভেরিয়েবল শুধু ওই ফাংশনে কাজ করে
   Block Scope → {} ব্লকের ভিতরে declare করা ভেরিয়েবল শুধু ওই ব্লকে কাজ করে

8. প্রশ্ন: ব্লক স্কোপ কি জিনিস? let, const কি টাইপের স্কোপ তৈরি করে?
   উত্তর:
   let, const → block scope তৈরি করে
   var → function scope তৈরি করে

9. প্রশ্ন: (এডভান্সড) Closure কি জিনিস? এইটা কিভাবে কাজ করে?
   উত্তর:
   Closure = function + lexical scope
   Inner function outer function-এর ভেরিয়েবল access করতে পারে।
   উদাহরণ:
   function outer() {
     let count = 0;
     return function inner() {
       count++;
       return count;
     }
   }

10. প্রশ্ন: Callback function কি জিনিস?
    উত্তর:
    অন্য ফাংশনের argument হিসেবে পাঠানো ফাংশন।
    উদাহরণ:
    setTimeout(() => { console.log("Hello"); }, 1000);

11. প্রশ্ন: Hoisting কি জিনিস?
    উত্তর:
    ভেরিয়েবল ও ফাংশন declaration উপরে তুলে আনা হয়।
    উদাহরণ:
    console.log(a); // undefined
    var a = 5;

12. প্রশ্ন: (এডভান্সড) কি ধরণের ভেরিয়েবল reference দিয়ে ফাংশনে পাঠানো হয় আর কোন ধরণের ভেরিয়েবল value হিসেবে পাঠানো হয়?
    উত্তর:
    Primitive types → pass by value
    Non-primitive types (object, array, function) → pass by reference

